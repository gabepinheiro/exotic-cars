import styled from 'styled-components'

export const Wrapper = styled.div`
  padding: 2.2rem 0;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(28.7rem, 1fr));
  grid-gap: 3.2rem 2.4rem;

  a {
    text-decoration: none;
    color: currentColor;
  }
`
