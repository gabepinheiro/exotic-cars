import styled from 'styled-components'

export const Wrapper = styled.div`
  padding: 2.2rem 0;
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(28.7rem, max-content));
  grid-gap: 2.2rem 3.2rem;
`
