import styled from 'styled-components'

export const Wrapper = styled.h1`
  font-size: 2.4rem;
  text-transform: uppercase;
  cursor: pointer;

  span {
    font-size: 1.6rem;
    font-weight: lighter;
    margin-left: 6px;
  }
`
